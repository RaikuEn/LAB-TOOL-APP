25/2/2026 ver 0.2

added:
login page and authorization

need to +:
history button
add tools button or page or pop up
log out button
dashboard to track borrowed items

26/2/2026 ver 0.3

added:
log out button
history button and history view
add tools button or page or pop up

trouble:
1. white screen because forgot to declare the showhistory.
2. token is not read by the return patch. solution, the code structure is wrong, it should be name, body, token, but only name, body.

need to +:
dashboard to track borrowed items
admin privilege to add and remove user
make the app self service to borrow but need admin to return
need to make it accessible to multiple user at the same time without interfering with each other
optimize the tools layout into a table that can be sorted by name or category
create a dedicated page for each feature,e.g,history, available tools list, borrowed items dashboard with return button with partial return or returned broken.